apiVersion: skaffold/v2beta28
kind: Config
metadata:
  name: kube-backlab
build:
  artifacts:
  - image: hello-app
    context: "{{.PROJECT_PATH}}"
    docker:
      dockerfile: Dockerfile
  local:
    push: false
deploy:
  kubectl:
    manifests:
    - k8s/*.yaml
