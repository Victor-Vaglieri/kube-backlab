apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: dev
data:
  PORT: "3000"
  DB_HOST: "postgres-postgresql.infra.svc.cluster.local"
  DB_PORT: "5432"
  DB_USER: "postgres"
  DB_NAME: "postgres"
